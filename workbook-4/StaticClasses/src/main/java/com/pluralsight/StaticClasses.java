package com.pluralsight;

public class StaticClasses{
    public static  String format(String prefix,String firstName,String middleName,String lastName, String suffix){
        return (prefix != null ? prefix + " " : " ") + firstName + ()
    }
}
